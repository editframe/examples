# audio/

This directory holds audio assets for future SFX + music pass.

Currently empty — the `output/demo.mp4` is silent.

## Future pass

Run an SFX bash mix script (pattern: `add-sfx-v1.sh`) using ffmpeg adelay to place:
- Keyboard click on `clerk login` command submit
- Subtle whoosh on camera zoom-in moments
- Soft notify tone on "Clerk has been set up in your project" success line

Music: ambient modern tech track, ~85-100 BPM, minor key. See `brand-rules-clerk.md` vibe notes.
