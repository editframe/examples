# OpenClaw - From Message to Done

A 30-second product demo of OpenClaw turning one chat message into completed work across mail, calendar, GitHub, and local device state.

Built with the [Editframe](https://editframe.com) React SDK. 1920x1080 at 30fps.

Includes an original synthesized audio bed.

## Quick start

```bash
npm install
npm run render
```

`npm run render` renders the visual pass with Editframe, then muxes the score
with `ffmpeg`. The final video is saved to `output/demo.mp4`.

See [CREDITS.md](CREDITS.md) for asset, font, and audio notes.
