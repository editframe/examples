/**
 * scene-assets.ts — shared asset paths for scenes.
 * Real files under src/assets/, served at /codex-demo/src/assets/<name> (namespaced
 * per-example since one shared vite-plugin root now serves every example — see the
 * composition skills' local-assets convention). No base64 embedded in source.
 */
export const codexGradientSrc = "/codex-demo/src/assets/codex-gradient.png";
export const cursorMacosSrc = "/codex-demo/src/assets/cursor-macos.svg";
